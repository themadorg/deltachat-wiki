---
title: مشارکت در deltachat.wiki
description: چگونه در ویکی دلتاچت مشارکت کنید — اصلاح خطاها، افزودن محتوا و کمک به ترجمه مستندات به زبان‌های بیشتر.
category: شروع کار
order: 6
---

# مشارکت در deltachat.wiki

از علاقه شما به مشارکت در **deltachat.wiki** متشکریم! این یک پروژه مستندات غیررسمی و جامعه‌محور برای [Delta Chat](https://delta.chat) است و هر مشارکتی آن را برای همه بهتر می‌کند.

این پروژه تحت مجوز **GNU GPLv3 (کپی‌لفت)** منتشر شده — شما آزادید که از آن استفاده کنید، تغییر دهید و به اشتراک بگذارید.

---

## نحوه مشارکت

### ۱. فورک کردن مخزن

تمام مشارکت‌ها از طریق **Pull Request** انجام می‌شود. ابتدا پروژه را فورک کنید:

1. به [github.com/themadorg/deltachat-wiki](https://github.com/themadorg/deltachat-wiki) بروید
2. دکمه **Fork** را در گوشه بالا-راست بزنید
3. فورک خود را به صورت محلی کلون کنید:
   ```sh
   git clone https://github.com/YOUR_USERNAME/deltachat-wiki.git
   cd deltachat-wiki
   ```
4. وابستگی‌ها را نصب کنید:
   ```sh
   bun install
   ```
5. سرور توسعه را اجرا کنید:
   ```sh
   bun run dev
   ```

### ۲. اعمال تغییرات

یک شاخه جدید بسازید، تغییرات خود را اعمال کنید و کامیت کنید:

```sh
git checkout -b my-contribution
# ... تغییرات خود را اعمال کنید ...
git add .
git commit -m "docs: توضیح تغییرات"
```

### ۳. ارسال Pull Request

شاخه خود را پوش کنید و یک PR به شاخه `main` باز کنید:

```sh
git push origin my-contribution
```

سپس به فورک خود در GitHub بروید و روی **"Compare & pull request"** کلیک کنید.

---

## گزارش و اصلاح خطاها

### خطاهای مفهومی یا واقعی

اگر محتوایی پیدا کردید که **از نظر واقعی نادرست** است یا مفهومی را اشتباه توضیح می‌دهد:

1. **ابتدا آن را در نسخه انگلیسی (`en`) اصلاح کنید** — انگلیسی زبان مرجع اصلی است
2. در توضیحات PR خود، **مشخص کنید چه چیزی اشتباه بود و چه اصلاحی انجام دادید**
3. **درخواست کنید که اصلاح در تمام زبان‌های دیگر هم اعمال شود** با افزودن یادداشتی مانند:

> ⚠️ این PR یک خطای واقعی در `src/docs/en/general/privacy.md` را اصلاح می‌کند. همین اصلاح باید در نسخه‌های `fa`، `ru` و `es` نیز اعمال شود.

این کار تضمین می‌کند که اطلاعات نادرست در نسخه‌های ترجمه‌شده باقی نمی‌ماند.

### خطاهای ترجمه

اگر متوجه شدید که یک **ترجمه با معنای متن اصلی انگلیسی مطابقت ندارد**:

1. **یک Issue باز کنید** یا PR ارسال کنید
2. مشخص کنید:
   - کدام فایل تحت تأثیر است
   - کدام زبان خطا دارد
   - متن فعلی چیست
   - متن صحیح چه باید باشد

**مثال Issue:**

> 🌐 خطای ترجمه در `src/docs/fa/general/privacy.md`
> پاراگراف مربوط به رمزنگاری سرتاسری در حال حاضر می‌گوید "..." اما متن اصلی انگلیسی می‌گوید "...". ترجمه صحیح فارسی باید "..." باشد.

### اشتباهات تایپی و دستوری

- **اشتباهات تایپی انگلیسی:** مستقیماً اصلاح کنید و PR ارسال کنید
- **زبان‌های دیگر:** اگر آن زبان را بلدید، مستقیماً اصلاح کنید. در غیر این صورت یک Issue باز کنید و توضیح دهید چه چیزی اشتباه به نظر می‌رسد و در کدام فایل/زبان

---

## نوشتن مستندات

### ساختار فایل

صفحات مستندات فایل‌های Markdown با سربرگ YAML هستند:

```markdown
---
title: عنوان صفحه
description: توضیح مختصر محتوای صفحه
category: نام دسته‌بندی
order: 1
---

محتوای شما اینجا قرار می‌گیرد...
```

### محل قرار دادن فایل‌ها

```
src/docs/
├── en/          ← انگلیسی (مرجع اصلی)
│   ├── general/
│   ├── webxdc/
│   ├── bot/
│   └── servers/
├── fa/          ← ترجمه‌های فارسی
├── ru/          ← ترجمه‌های روسی
└── es/          ← ترجمه‌های اسپانیایی
```

### ناوبری سایدبار

هر بخش یک فایل `config.ts` دارد که ترتیب صفحات در سایدبار را مشخص می‌کند:

```typescript
export const sidebarConfig = [
    { title: "معرفی", slug: "general/introduction" },
    { title: "ویژگی‌ها", slug: "general/features" },
    // صفحه جدید خود را اینجا اضافه کنید
];
```

**فراموش نکنید که هنگام افزودن صفحه جدید، `config.ts` را به‌روزرسانی کنید!**

---

## راهنمای ترجمه

این ویکی به زبان‌های **انگلیسی**، **فارسی**، **روسی** و **اسپانیایی** در دسترس است.

### قوانین برای مترجمان

1. **همیشه از متن انگلیسی ترجمه کنید** — هرگز از ترجمه زبان دیگری ترجمه نکنید
2. **مسیر فایل را حفظ کنید** — `en/general/features.md` → `fa/general/features.md`
3. **مقادیر سربرگ را ترجمه کنید** — `title` و `description` را ترجمه کنید، بقیه کلیدها را تغییر ندهید
4. **بلوک‌های کد را ترجمه نکنید** — نمونه‌های کد به شکل اصلی باقی می‌مانند
5. **اصطلاحات فنی را حفظ کنید** مگر اینکه معادل محلی پذیرفته‌شده‌ای وجود داشته باشد
6. **اسلاگ‌های URL را به انگلیسی نگه دارید** — مسیرها باید در همه زبان‌ها یکسان باشند
7. **رشته‌های رابط کاربری را به‌روزرسانی کنید** — اگر کلید جدیدی به `en.json` اضافه کردید، آن را به فایل‌های زبان دیگر هم اضافه کنید

### فایل‌های ترجمه رابط کاربری

رابط کاربری سایت (ناوبری، دکمه‌ها، پاورقی و غیره) از طریق فایل‌های JSON ترجمه می‌شود:

```
src/lib/i18n/
├── en.json    ← مرجع
├── fa.json
├── ru.json
└── es.json
```

---

## افزودن زبان جدید

می‌خواهید پشتیبانی از زبان جدیدی اضافه کنید؟ مراحل:

1. زبان را به `src/lib/languages.ts` اضافه کنید:
   ```typescript
   { code: 'de', name: 'Deutsch', dir: 'ltr' }
   ```
2. فایل `src/lib/i18n/de.json` را بسازید — از `en.json` کپی کنید و ترجمه کنید
3. ترجمه جدید را در `src/lib/i18n.svelte.ts` ثبت کنید
4. ترجمه مستندات را در `src/docs/de/` شروع کنید
5. فایل‌های `config.ts` سایدبار را برای هر بخش بسازید

سیستم بیلد به صورت خودکار زبان جدید را برای مسیریابی، پیش‌رندر و سوئیچ زبان شناسایی می‌کند.

---

## راهنمای Pull Request

### قرارداد عنوان PR

از پیشوندهای واضح و استاندارد استفاده کنید:

| پیشوند | کاربرد |
|--------|---------|
| `docs:` | افزودن یا به‌روزرسانی محتوای مستندات |
| `fix:` | اصلاح خطاهای واقعی یا باگ‌ها |
| `i18n:` | افزودن ترجمه‌های جدید |
| `i18n-fix:` | اصلاح خطاهای ترجمه |
| `feat:` | افزودن ویژگی‌ها یا قابلیت‌های جدید |

### چک‌لیست قبل از ارسال

- [ ] تغییرات بدون خطا بیلد می‌شوند (`bun run build`)
- [ ] محتوا دقیق و خوب‌نوشته است
- [ ] سربرگ (`title`، `description`، `category`، `order`) صحیح است
- [ ] `config.ts` سایدبار به‌روزرسانی شده (در صورت افزودن صفحه جدید)
- [ ] اگر خطای محتوایی اصلاح شده، زبان‌های دیگر برای به‌روزرسانی علامت‌گذاری شده‌اند
- [ ] توضیحات PR مشخص می‌کند چه تغییری داده شده و چرا

---

## سؤالی دارید؟

- یک [Issue در GitHub](https://github.com/themadorg/deltachat-wiki/issues) باز کنید
- به [انجمن پشتیبانی Delta Chat](https://support.delta.chat/) بپیوندید

از کمک شما در دسترس‌تر کردن مستندات دلتاچت برای همه متشکریم! ❤️
