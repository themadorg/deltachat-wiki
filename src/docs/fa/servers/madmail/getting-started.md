---
title: شروع کار با Madmail
description: نحوه استقرار سرور Madmail شخصی خود برای دلتاچت.
category: سرورها
---

<script>
    import Steps from '$lib/components/Steps.svelte';
    import Step from '$lib/components/Step.svelte';
</script>

# راه‌اندازی سرور Madmail

Madmail برای استقرار سریع طراحی شده است، به‌ویژه در محیط‌های شبکه‌ای محدود. این راهنما توضیح می‌دهد که چگونه سرور خود را به‌سرعت راه‌اندازی کنید.

## حداقل الزامات

- یک سرور خام با **Debian 12** یا **Ubuntu**.
- یک آدرس IP عمومی (IPv4 یا IPv6).
- ۱ گیگابایت رم و ۵ تا ۱۰ گیگابایت فضای ذخیره‌سازی معمولاً کافی است.

## استقرار سریع (مبتنی بر IP)

Madmail از استقرار مستقیم مبتنی بر IP پشتیبانی می‌کند تا مشکلات مربوط به DNS را دور بزند.

<Steps>
<Step number="1" title="نصب خودکار">

دستور زیر را در سرور خود اجرا کنید تا Madmail را با استفاده از IP عمومی خود به‌سرعت نصب کنید:

```bash
wget https://github.com/themadorg/madmail/releases/latest/download/madmail && \
chmod +x madmail && \
sudo ./madmail install --simple --ip [آدرس_آی‌پی_عمومی_شما] && \
sudo systemctl start maddy
```

</Step>
<Step number="2" title="نصب دستی فایل اجرایی" isLast={true}>

اگر فایل اجرایی `madmail` را به‌صورت محلی دارید:

۱. **آپلود فایل**:
   ```bash
   scp madmail root@[آدرس_آی‌پی_سرور]:/root/
   ```
۲. **اجرای نصب**:
   ```bash
   ssh root@[آدرس_آی‌پی_سرور] "chmod +x /root/madmail && ./root/madmail install --simple --ip [آدرس_آی‌پی_سرور] && systemctl start maddy"
   ```

</Step>
</Steps>

*بجای `[آدرس_آی‌پی_سرور]`، آدرس IP واقعی سرور خود را جایگزین کنید.*

## پیکربندی پیشرفته

### مدیریت ثبت‌نام خودکار (JIT)
ثبت‌نام JIT (مخفف Just-In-Time) اجازه می‌دهد حساب‌ها به‌طور خودکار هنگام اولین ورود کاربر ایجاد شوند.

```bash
# غیرفعال کردن ایجاد خودکار حساب
sudo maddy --config /etc/maddy/maddy.conf creds jit disable

# فعال کردن ایجاد خودکار حساب
sudo maddy --config /etc/maddy/maddy.conf creds jit enable

# بررسی وضعیت
sudo maddy --config /etc/maddy/maddy.conf creds jit status
```

### پورت‌های مورد نیاز
مطمئن شوید پورت‌های زیر در دیوار آتش (Firewall) شما باز هستند:
- **80 / 443**: ثبت‌نام وب و رابط کاربری ورود.
- **25**: SMTP (تعامل با سایر سرورها).
- **465 / 587**: SMTP Submission (ارسال پیام).
- **143 / 993**: IMAP (دریافت پیام).
- **3340**: رله Iroh (لازم برای ویژگی‌های لحظه‌ای Webxdc).

## به‌روزرسانی Madmail
دستور داخلی `update` به‌طور خودکار امضاهای دیجیتال را تأیید می‌کند:

```bash
sudo maddy update https://github.com/themadorg/madmail/releases/latest/download/madmail
```

## حمایت جامعه
برای جدیدترین فایل‌های اجرایی و نکات:
👉 [کانال تلگرام Madmail](https://t.me/the_madmail)
