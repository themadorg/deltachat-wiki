---
title: Участие в deltachat.wiki
description: Как внести свой вклад в вики Delta Chat — исправить ошибки, добавить контент и помочь с переводом документации на другие языки.
category: Начало работы
order: 6
---

# Участие в deltachat.wiki

Спасибо за ваш интерес к проекту **deltachat.wiki**! Это неофициальный проект документации для [Delta Chat](https://delta.chat), созданный сообществом. Каждый вклад помогает сделать его лучше для всех.

Проект распространяется под лицензией **GNU GPLv3 (Copyleft)** — вы можете свободно использовать, изменять и распространять его.

---

## Как участвовать

### 1. Форк репозитория

Все изменения вносятся через **Pull Request**. Начните с форка проекта:

1. Перейдите на [github.com/themadorg/deltachat-wiki](https://github.com/themadorg/deltachat-wiki)
2. Нажмите кнопку **Fork** в правом верхнем углу
3. Клонируйте ваш форк локально:
   ```sh
   git clone https://github.com/YOUR_USERNAME/deltachat-wiki.git
   cd deltachat-wiki
   ```
4. Установите зависимости:
   ```sh
   bun install
   ```
5. Запустите сервер разработки:
   ```sh
   bun run dev
   ```

### 2. Внесите изменения

Создайте новую ветку, внесите правки и закоммитьте:

```sh
git checkout -b my-contribution
# ... внесите изменения ...
git add .
git commit -m "docs: описание изменений"
```

### 3. Отправьте Pull Request

Запушьте ветку и откройте PR в ветку `main`:

```sh
git push origin my-contribution
```

Затем перейдите в ваш форк на GitHub и нажмите **"Compare & pull request"**.

---

## Сообщение об ошибках и их исправление

### Фактические или концептуальные ошибки

Если вы нашли контент, который **фактически неверен** или неправильно описывает концепцию:

1. **Сначала исправьте в английской (`en`) версии** — английский является основным исходным языком
2. В описании PR **чётко объясните, что было неправильно и что вы исправили**
3. **Запросите применение исправления во всех других языках**, добавив примечание:

> ⚠️ Этот PR исправляет фактическую ошибку в `src/docs/en/general/privacy.md`. Это же исправление нужно применить к версиям на `fa`, `ru` и `es`.

Это гарантирует, что неверная информация не останется в переведённых версиях.

### Ошибки перевода

Если вы заметили, что **перевод не соответствует смыслу** английского оригинала:

1. **Откройте Issue** или отправьте PR
2. Укажите:
   - Какой файл затронут
   - В каком языке ошибка
   - Что текст говорит сейчас
   - Что он должен говорить

**Пример Issue:**

> 🌐 Ошибка перевода в `src/docs/ru/general/privacy.md`
> Параграф о сквозном шифровании сейчас говорит "..." но в английском оригинале сказано "...". Правильный русский перевод должен быть "..."

### Опечатки и грамматика

- **Опечатки в английском тексте:** исправьте напрямую и отправьте PR
- **Другие языки:** если вы знаете язык, исправьте напрямую. Если нет, откройте Issue с описанием того, что выглядит неправильно и в каком файле/языке

---

## Написание документации

### Структура файлов

Страницы документации — это файлы Markdown с YAML-заголовком:

```markdown
---
title: Заголовок страницы
description: Краткое описание содержания страницы
category: Название категории
order: 1
---

Ваш контент здесь...
```

### Где размещать файлы

```
src/docs/
├── en/          ← Английский (основной источник)
│   ├── general/
│   ├── webxdc/
│   ├── bot/
│   └── servers/
├── fa/          ← Переводы на персидский
├── ru/          ← Переводы на русский
└── es/          ← Переводы на испанский
```

### Навигация в боковой панели

Каждый раздел имеет файл `config.ts`, определяющий порядок страниц в боковой панели:

```typescript
export const sidebarConfig = [
    { title: "Введение", slug: "general/introduction" },
    { title: "Возможности", slug: "general/features" },
    // Добавьте новую страницу сюда
];
```

**Не забудьте обновить `config.ts`** при добавлении новой страницы!

---

## Рекомендации по переводу

Эта вики доступна на **английском**, **персидском**, **русском** и **испанском** языках.

### Правила для переводчиков

1. **Всегда переводите с английского оригинала** — никогда не переводите с другого перевода
2. **Сохраняйте ту же структуру файлов** — `en/general/features.md` → `ru/general/features.md`
3. **Переводите значения заголовка** — переводите `title` и `description`, остальные ключи оставляйте без изменений
4. **Не переводите блоки кода** — примеры кода остаются в исходном виде
5. **Сохраняйте технические термины** в оригинале, если нет общепринятого местного эквивалента
6. **Оставляйте URL-слаги на английском** — пути должны быть одинаковыми во всех языках
7. **Обновляйте строки интерфейса** — если вы добавили новый ключ в `en.json`, добавьте его и в файлы других языков

### Файлы перевода интерфейса

Интерфейс сайта (навигация, кнопки, подвал и т.д.) переводится через JSON-файлы:

```
src/lib/i18n/
├── en.json    ← Источник
├── fa.json
├── ru.json
└── es.json
```

---

## Добавление нового языка

Хотите добавить поддержку нового языка? Вот как:

1. Добавьте язык в `src/lib/languages.ts`:
   ```typescript
   { code: 'de', name: 'Deutsch', dir: 'ltr' }
   ```
2. Создайте `src/lib/i18n/de.json` — скопируйте из `en.json` и переведите
3. Зарегистрируйте перевод в `src/lib/i18n.svelte.ts`
4. Начните переводить документы в `src/docs/de/`
5. Создайте файлы `config.ts` боковой панели для каждого раздела

Система сборки автоматически подхватит новый язык для маршрутизации, пре-рендеринга и переключателя языков.

---

## Рекомендации для Pull Request

### Соглашение о заголовках PR

Используйте чёткие стандартные префиксы:

| Префикс | Для чего |
|---------|----------|
| `docs:` | Добавление или обновление содержания документации |
| `fix:` | Исправление фактических ошибок или багов |
| `i18n:` | Добавление новых переводов |
| `i18n-fix:` | Исправление ошибок перевода |
| `feat:` | Добавление новых функций или возможностей |

### Чек-лист перед отправкой

- [ ] Изменения собираются без ошибок (`bun run build`)
- [ ] Контент точный и хорошо написан
- [ ] Заголовок (`title`, `description`, `category`, `order`) корректен
- [ ] `config.ts` боковой панели обновлён (при добавлении новой страницы)
- [ ] Если исправлена ошибка контента, другие языки отмечены для обновления
- [ ] Описание PR объясняет, что изменено и почему

---

## Есть вопросы?

- Откройте [Issue на GitHub](https://github.com/themadorg/deltachat-wiki/issues)
- Присоединяйтесь к [Форуму поддержки Delta Chat](https://support.delta.chat/)

Спасибо за помощь в том, чтобы сделать документацию Delta Chat доступной для всех! ❤️
