---
title: Начало работы с Madmail
description: Как развернуть собственный сервер Madmail для Delta Chat.
category: Серверы
---

<script>
    import Steps from '$lib/components/Steps.svelte';
    import Step from '$lib/components/Step.svelte';
</script>

# Настройка сервера Madmail

Madmail разработан для быстрого развертывания, особенно в условиях ограниченного сетевого доступа. Это руководство объяснит, как быстро запустить ваш сервер.

## Минимальные требования

- Чистый сервер с **Debian 12** или **Ubuntu**.
- Публичный IP-адрес (IPv4 или IPv6).
- 1 ГБ оперативной памяти и 5-10 ГБ на диске обычно достаточно.

## Быстрое развертывание (по IP-адресу)

Madmail поддерживает прямое развертывание по IP-адресу для обхода проблем, связанных с DNS.

<Steps>
<Step number="1" title="Автоматическая установка">

Запустите следующую команду на вашем сервере для быстрой установки Madmail с использованием вашего публичного IP-адреса:

```bash
wget https://github.com/themadorg/madmail/releases/latest/download/madmail && \
chmod +x madmail && \
sudo ./madmail install --simple --ip [ВАШ_ПУБЛИЧНЫЙ_IP] && \
sudo systemctl start maddy
```

</Step>
<Step number="2" title="Ручная установка бинарного файла" isLast={true}>

Если у вас есть бинарный файл `madmail` на локальном компьютере:

1. **Загрузите файл на сервер**:
   ```bash
   scp madmail root@[IP_ВАШЕГО_СЕРВЕРА]:/root/
   ```
2. **Запустите установку**:
   ```bash
   ssh root@[IP_ВАШЕГО_СЕРВЕРА] "chmod +x /root/madmail && ./root/madmail install --simple --ip [IP_ВАШЕГО_СЕРВЕРА] && systemctl start maddy"
   ```

</Step>
</Steps>

*Замените `[IP_ВАШЕГО_СЕРВЕРА]` вашим реальным IP-адресом.*

## Расширенная настройка

### Управление автоматической регистрацией (JIT)
Регистрация JIT (Just-In-Time) позволяет автоматически создавать учетные записи при первом входе пользователя.

```bash
# Включить автоматическое создание учетных записей
sudo maddy --config /etc/maddy/maddy.conf creds jit enable

# Выключить автоматическое создание учетных записей
sudo maddy --config /etc/maddy/maddy.conf creds jit disable

# Проверить статус
sudo maddy --config /etc/maddy/maddy.conf creds jit status
```

### Необходимые порты
Убедитесь, что в вашем брандмауэре открыты следующие порты:
- **80 / 443**: веб-интерфейс регистрации и подключения.
- **25**: SMTP (федерация с другими серверами).
- **465 / 587**: SMTP Submission (отправка сообщений).
- **143 / 993**: IMAP (получение сообщений).
- **3340**: реле Iroh (необходимо для функций реального времени Webxdc).

## Обновление Madmail
Встроенная команда `update` автоматически проверяет цифровые подписи:

```bash
sudo maddy update https://github.com/themadorg/madmail/releases/latest/download/madmail
```

## Поддержка сообщества
Для получения последних версий и советов:
👉 [Telegram-канал Madmail](https://t.me/the_madmail)
