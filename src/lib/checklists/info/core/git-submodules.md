---
layout: checklist-info
---

# Git submodules initialized (`git submodule update --init`)

Run after cloning: git submodule update --init --recursive
Several native dependencies are vendored as submodules.
