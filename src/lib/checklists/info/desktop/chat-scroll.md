---
layout: checklist-info
---

# Chat view scrolls and loads older messages

`MessageList` bidirectional infinite scroll loads older and newer messages (react-window).

**Verify:** scroll up in long chat; older messages load without gap.
