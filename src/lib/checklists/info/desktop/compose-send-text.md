---
layout: checklist-info
---

# Send text message in active chat

`Composer` + `ComposerMessageInput`; **Enter** sends when `enterKeySends` is on.

**Verify:** type and send text; message appears in bubble and on recipient device.
