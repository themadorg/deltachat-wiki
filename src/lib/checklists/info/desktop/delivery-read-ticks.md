---
layout: checklist-info
---

# Delivery and read tick marks on outgoing messages

`MapMsgStatus` shows sent/delivered/read ticks. Read ticks require **Read receipts** (`mdns_enabled`) enabled on both sides.

**Verify:** send message; one tick on send; two ticks after recipient reads (with read receipts on).
