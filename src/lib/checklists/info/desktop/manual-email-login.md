---
layout: checklist-info
---

# Log in with email and password (IMAP/SMTP)

Alternative logins → manual email (`AccountSetupScreen` + `LoginForm`). IMAP/SMTP credentials, provider hints on email blur, `ConfigureProgressDialog` on success.

**Verify:** log in with existing IMAP account; chats sync; credentials persist after restart.
