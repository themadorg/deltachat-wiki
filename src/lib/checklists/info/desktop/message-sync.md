---
layout: checklist-info
---

# Incoming messages sync into the chat list

Core fetches mail over IMAP; chat list updates as `IncomingMsg` events arrive.

**Verify:** send from second device; message appears in chat list without manual refresh.
