---
layout: checklist-info
---

# Opening running app focuses existing window

One window per `{accountId}.{messageId}`. Re-open restores minimized window and **focuses** existing instance instead of spawning duplicate.

**Verify:** open app; minimize; click Start app again; same window restores and focuses.
