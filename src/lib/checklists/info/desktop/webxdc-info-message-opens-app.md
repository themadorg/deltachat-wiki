---
layout: checklist-info
---

# Click Webxdc info message opens parent app

`WebxdcInfoMessage` rows (app updates/notifications). **Click** opens or focuses parent app via `parentId`; may pass `webxdcHref` for in-app route.

**Verify:** click webxdc info message; parent app window opens or focuses.
