---
layout: checklist-info
---

# Desktop notification on Webxdc app notify event

`IncomingWebxdcNotify` shows native notification with app name; icon from `getWebxdcBlob` when available. Respects global and per-chat mute.

**Verify:** trigger webxdc notify from running app; OS notification appears with app name.
