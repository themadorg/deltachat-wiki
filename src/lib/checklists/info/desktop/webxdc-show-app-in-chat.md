---
layout: checklist-info
---

# Show app in chat jumps to parent app message

Info message context menu → **Show app in chat** (`show_app_in_chat`). Navbar icon right-click → **Show in chat** → `jumpToMessage` with highlight.

**Verify:** right-click info message → Show app in chat; scrolls to parent Webxdc message.
