---
layout: checklist-info
---

# Two users exchange PGP-encrypted 1:1 messages

Two accounts on the relay (or one local, one remote) must send and receive encrypted chat messages with correct threading and read receipts. Primary functional test for SMTP submission + IMAP delivery + PGP policy.