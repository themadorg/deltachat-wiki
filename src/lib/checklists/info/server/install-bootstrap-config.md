---
layout: checklist-info
---

# Install or bootstrap command creates initial config

First-run setup must produce a working config file or database settings: hostname, listener ports, admin token, storage paths, and feature toggles. Operator should not hand-edit dozens of keys before the first start.