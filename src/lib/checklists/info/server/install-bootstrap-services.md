---
layout: checklist-info
---

# Install or bootstrap command starts mail services

First-run setup must start (or enable) SMTP, IMAP, and HTTPS listeners — or print clear instructions to start them via your process supervisor. TLS key material should be generated or imported during bootstrap when using ACME or self-signed defaults.