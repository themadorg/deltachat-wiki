---
layout: checklist-info
---

# Install or bootstrap command initializes mail storage

First-run setup must create the mailbox database schema, quota tables, and on-disk mail spool (or object store) with correct permissions. Empty install should be ready to accept JIT registration.