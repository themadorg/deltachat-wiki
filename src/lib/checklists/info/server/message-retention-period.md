---
layout: checklist-info
---

# Configurable message retention period

Operators can set how long messages are kept per mailbox or server-wide. Retention may be unlimited by default; document behaviour when a limit is configured.

Retention policy should apply consistently across SMTP delivery, `/mxdeliv`, and IMAP-visible mail.